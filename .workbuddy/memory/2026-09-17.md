# 2026-09-17

## 冷链运输时间差8小时修复
- 排查：实际发车/到达时间由后端 depart()/arrive() 写入 LocalDateTime.now()（DistributionServiceImpl.java:339/377），前端 TransportMonitor.vue 原样透传，无前端取当前时间的逻辑
- 根因：JVM 时区未显式设置，运行在 UTC 环境导致业务时间偏差 8 小时
- 修复：新增 backend/common/common-web/src/main/java/com/pork/web/config/TimezoneConfig.java（@PostConstruct 设 TimeZone.setDefault("Asia/Shanghai")），注册到 CommonWebAutoConfiguration
- 待办：需重新构建部署后端生效；已写入的偏差历史数据需手工修正或重新执行 init-sql
- 环境备注：本会话 bash 核心 coreutils（ls/tail/which 等）缺失，PowerShell 工具 stdout 无法回显，Maven 编译验证未能执行

## 删除销售页面链上状态列
- web-admin/src/views/sales/SaleRecords.vue 移除 blockHash"链上状态"表格列；详情弹窗中"链上哈希"字段保留

## 发起召回报"不能为空"修复
- 根因：后端 SalesRequests.RecallCreate 要求 scope(@NotEmpty Map) 与 initiator(@NotBlank) 必填，前端 RecallManage.vue 只发了 batchIds/storeIds/note 顶层字段
- 修复：前端提交改为 scope:{batchIds,storeIds,note}，initiator 取 authStore.user.realName/username；后端 longList 可正确解析 JSON 数字数组，详情弹窗 handleView 也按 scope.batchIds/storeIds 解析，无需改后端

## 小程序 emoji 排查
- 检查发现 mini-app/src 源码已无 emoji（icon 均为空串），dist/dev/mp-weixin 是旧构建残留 emoji（🐷🔪🍖🚚🏪✅⚠️等）
- 处理：重跑 npm run dev:mp-weixin 刷新 dist，业务代码 emoji 已清零；vendor.js 中仅剩 Pinia 库自身源码的 🍍（调试日志，不显示在 UI，不处理）
- 环境备注：bash 下 npm 会解析到 wsl.exe 被安全策略拦截，npm/构建需用 PowerShell 执行

## 已上链数据"作废+重录"改造（slaughter-service）
- 范围：SlaughterInspection（屠宰检验，SLAUGHTER_INSPECT 上链）、CarcassStamp（检疫盖章，SLAUGHTER_INSPECT 上链）；EntryInspection/RactopamineTest 未上链不动
- 后端：检验状态新增 3=已作废（SlaughterStatus）；新增 PUT /{id}/void 作废端点（作废动作以独立 bizKey xxx:VOID、独立 bizType *_VOID 单独上链，不影响原记录哈希验真）；update 编辑后重新 publish 新哈希（chain_ledger 按 bizKey 取最新，版本化）；物理删除接口改为抛错引导作废；编辑禁止把状态改成已作废、已作废记录禁止编辑；溯源 internal/pigs 查询过滤已作废记录
- 前端：InspectTable/StampTable 加"作废"按钮（确认弹窗+PUT void+刷新）；检验状态筛选与标签加"已作废"；StampFormDialog 移除"已作废"选项
- 验证：本机无 Maven 无法编译，需用户本地构建；审计日志页对 *_VOID 新 bizType 无中文映射（显示原文，可后续补充）

## Docker Desktop 部署生效
- 后端以 docker compose 部署（Dockerfile 多阶段构建，Maven 在镜像内执行，本机无需 Maven）
- docker-compose.yml 的 x-app-environment 增加 TZ=Asia/Shanghai（此前仅 mysql 有，Java 容器为 UTC，是差 8 小时的直接原因）
- 已执行 docker compose build（9 个服务镜像全部重建成功）+ docker compose up -d，全部 healthy；distribution 容器内 date 显示 CST 正常
- 网关路由带 /api/v1 前缀（nginx 转发 /api/v1/ → gateway:8080），void 接口路由验证通过（401 未认证=鉴权拦截，路由存在）
- 遗留：nginx 容器 healthy 检查内部 wget localhost 被拒（host 访问 80 端口正常），改动前即存在，与本次无关；历史偏差数据未修正（init-sql 用 MySQL NOW+TZ 正确，仅应用写入的旧行偏差）

## 召回进度上报功能
- 后端 PUT /sales/recalls/{id}/status 接口原本就存在（SalesRequests.RecallStatus：status 2-4 + recalledCount，校验不可回退/不超影响数），缺前端入口和上链存证
- SalesServiceImpl.updateRecall 增加进度变更上链：RECALL_ORDER_PROGRESS，bizKey=召回号:PROGRESS，不影响原召回指令验真
- 前端 RecallManage.vue：操作列加"进度上报"按钮（status 1/2 可见）+ 弹窗（执行中/已完成 + 已召回数）；详情弹窗补完成时间；sales.ts updateRecallStatus 放开 recalledCount
- 设计约定：已上链数据核心字段不可编辑，进度/作废等作为独立状态变更单独上链（独立 bizKey）；未上链页面（生猪档案/疫苗/入场查验/瘦肉精）编辑弹窗后端已有但列表无编辑按钮，无需删除功能（被下游引用）

## 生猪状态"在途"问题修复（ET-TEST-0016）
- 根因1（前端）：PigTable.vue 把生猪状态 2（后端枚举=已出栏）错误映射到 StatusTag 的 transport 类型，显示成运单的"在途"
- 根因2（后端）：生猪状态出栏审批后停在 2，屠宰/分割/签收各环节从不推进到 3=已屠宰
- 修复1：StatusTag 新增 soldOut(已出栏/warning) 类型，PigTable 映射改为 1raising/2soldOut/3slaughtered/4abnormal
- 修复2：breeding 新增内部接口 PUT /breeding/internal/pigs/{id}/status?status=n（只允许向前推进、幂等）；slaughter 宰后/同步检验创建时经 BreedingClient.advanceStatus 推进到 3；distribution 胴体批次 create/update 时对 pigIds 同步推进到 3（新建 distribution/client/BreedingClient，失败仅告警不阻塞）
- 存量修正：按宰后检验关联 UPDATE 6 行 + 按 carcass_batch.pig_ids JSON_CONTAINS 修正；ET-TEST-0016 现为 status=3，无遗漏数据
- 部署：breeding/slaughter/distribution 三服务镜像重建并重启 healthy（compose 报 exit 1 均为 PowerShell 重定向误报）

## 全项目编辑按钮接入
- 六个页面的编辑弹窗+后端 PUT 均为现成但列表无入口（editData 永远 null），统一补"编辑"按钮：PigTable→PigList、VaccineTable→VaccineRecord、EntryTable→EntryInspect（需同时置 dialogMode='edit'）、InspectTable→SlaughterInspect、RactoTable→RactopamineTest、StampTable→CarcassStamp；模式为"表格 $emit('edit',row) → 父页面 { ...row } 回填 currentEditData"
- 已上链的检验/盖章：编辑按钮仅 status 非已作废时显示（编辑后自动重新上链）；未上链四页面编辑无限制
- PigFormDialog 移除编辑态的物理"删除"按钮及 handleDelete/deleting（生猪被下游引用不可物理删除，与既定设计一致）
- 其余页面维持现状：运输单/分割/胴体批次已有编辑；销售/召回/二维码/投诉/预警/出栏申报为流程操作页无编辑需求；作废按钮仅检验/盖章两处（已上链）
- 修正：胴体批次实际无编辑入口，后补——CarcassBatch.vue 操作列加"编辑"（宽160→180），handleEdit(row) 回填表单+预置本批次生猪选项；后端 updateBatch 仅拦截已分割批次变更 pigIds
- 二维码管理操作列收窄（220→150）用户尚未确认，待办

## 疫苗凭证照片不显示修复
- 排查：db_breeding.vaccine_record 最新记录 file_ids=[]，db_common.file_metadata 无对应上传记录 → 照片从未到达服务端（昨日记录17有 fileId，链路本身通）
- 根因1（前端）：VaccineFormDialog 允许照片仍在上传/上传失败时提交记录，且上传失败无提示、成功回调不校验 code
- 修复1：新增 uploadingCount 计数，beforeUpload++ / success|error--；handleSubmit 拦截"上传中"并警告；on-success 校验 code!==200 或无 fileId 时报错；补 on-error、on-exceed（超1张提示）；resetForm 重置计数
- 根因2（基建）：nginx.conf 静态 proxy_pass 启动时缓存 gateway-service 旧 IP，网关容器重建后 80 端口全部 502（长期 unhealthy 的真因）
- 修复2：nginx.conf 加 resolver 127.0.0.11 valid=10s + set $gateway_upstream 变量代理；docker compose restart nginx 后验证 GET 401（正常鉴权）、upload 200

## 疫苗凭证二次修复：webp 被后端白名单拒绝
- 复现：curl 经 Vite 代理上传 webp → HTTP 200 + {code:1001, message:"不支持的文件类型"}（LocalFileStorageService 白名单仅 jpg/jpeg/png/pdf/mp4 + 魔数校验，BusinessException 被全局处理器包成 HTTP 200）
- 用户照片为 webp/heic 等格式 → el-upload 误判成功 → on-success 报"上传失败"，服务端无存储记录
- 修复（VaccineFormDialog.vue）：accept 改 .jpg/.jpeg/.png；beforeUpload 按扩展名校验并提示转格式；on-success 透出后端真实 message
- 环境备注：本机 wslrelay 占 [::1]:80/8080（与 docker 0.0.0.0 端口映射并存）；Vite dev 仅绑 [::1]:5173；curl 走系统代理 127.0.0.1:60910，测本机服务需 --noproxy "*"；web-admin 前端经 Vite dev server（5173）访问，不走 nginx

## 作废重录：生猪搜索过滤缺口修复
- 问题：作废屠宰检验/盖章后重录，生猪下拉搜不到（InspectFormDialog/StampFormDialog searchPigs）
- 原因1：只搜 status=2 已出栏，但检验已把猪推进为 3 已屠宰；原因2：排除名单把已作废记录也算作"已有记录"
- 修复：并行搜 status=2 和 3 合并去重；排除名单过滤掉已作废记录（检验 status!==3 / 盖章 status!==2）
- 另：检验编号唯一性校验（InspectFormDialog formRules）对已作废编号仍拦截，属有意设计（bizKey 不可复用），重录需换新编号
- 追加收窄：已屠宰猪仅在"存在已作废记录"的重录场景出现在下拉（voidedIds 放行），其余已屠宰猪（如经胴体批次直接推进、无检验记录）不可选，避免正常新建检验时误选已屠宰的猪

## 门店签收：移除签收人手动录入
- StoreReceipt.vue 签收弹窗删除"签收人"输入项（门店输入改为整行），删除"请输入签收人姓名"校验
- receiver 改为打开弹窗时自动取当前登录用户 realName/username，列表"签收人"列与详情展示保留、数据仍完整
- 用户澄清：上条改错了对象，实际要删的是电子签名。已恢复签收人手动录入+校验，改为删除电子签名：签名卡片/画布逻辑/签名校验/eSignature 提交/相关 CSS 全部移除；后端 ReceiptCreate.eSignature 去掉 @NotBlank（无业务引用，仅存储），distribution-service 镜像重建中

## 召回受影响数量只统计在售的问题修复
- RC20260917556108 affected_count=0 导致进度上报"已召回数量不能超出受影响数量"：创建召回时仅统计 status=1 在售，相关销售已售出(2)故为 0
- 修复：SalesServiceImpl createRecall 统计范围改为 status IN (1,2)（在售+已售出），命中后统一置 4=已召回
- 存量修正：recall_order id=18 affected_count 0→1；retail_sale id=30 status 2→4；sales-service 镜像重建部署
- 追加：updateRecall 中已召回数量达到受影响数量（>0）时自动置 status=3 已完成并记录 completedTime；链事件 status 改用最终 row.getStatus()
- 审计日志"RECALL_ORDER_PR"为 RECALL_ORDER_PROGRESS 列宽截断显示；AuditLog.vue 与 TraceSearch.vue 补齐三个新 bizType 中文映射：检验作废/盖章作废/召回进度
- AuditLog.vue 删除操作列（详情按钮仅弹提示，无实际功能）与统计栏"今日待确认"标签，清理未使用的 handleView/ElMessage/BlockchainVerifyBadge 引用

## 2026-09-18 补充
- 修复小程序溯源链路分割节点显示 'xxx · null'：TraceTimeline.vue desc 改为过滤空值后拼接
- 修复安心购'未通过宰后检验'误判：TraceQueryServiceImpl 新增 inspectionPass()，result 为空时用 status==1 兜底（宰前/宰后检验均生效）；重建 trace-service 镜像并重启（healthy），重跑 npm run build:h5，dist 已含修复
