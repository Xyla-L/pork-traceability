
## 2026-09-18 00:40 前后
- 修复小程序溯源链路 'xxx · null'（TraceTimeline.vue 过滤空值拼接）+ 安心购宰后检验误判（TraceQueryServiceImpl.inspectionPass：result 空时用 status==1 兜底）；重建 trace-service（healthy）、重跑 build:h5。
- 小程序举报页（complaint.vue）：删除"上传照片（选填）""联系方式（选填，便于反馈）"括号说明；提交时手机号格式校验 ^1[3-9]\d{9}$（选填，填了才校验）。
- web-admin 手机号校验统一加 /^1[3-9]\d{9}$/：TransportMonitor 司机电话（补 prop+rule）、OrgManage 联系电话（补 prop+rule）、PigFormDialog 养殖场联系电话（rule+maxlength 11）、UserManage 联系电话（必填+格式+maxlength 11）。Profile.vue 原有校验、SmsCaptcha 原有校验，StoreReceipt 无手机号输入框。
- 重跑 build:h5，dist 已包含举报页校验。

- 诊断 QR-PORK-53B5...（未改代码）：小程序时间轴末节点=门店签收，无'已出售'节点（数据里 sellTime/激活都有）；验真只按 qrCode(RETAIL_SALE)+splitBatchNo(SPLIT_BATCH) 两个 bizKey 查链，RETAIL_SALE 因上架激活+售出各上链一次产生两条 txHash，明细3条去重后2条，验真通过；该销售记录 status=4（被今日召回测试标记已召回）。

- 批量重置密码：DB 存量 18 个账号（admin/业务/retail_user_*）哈希是旧 ChangeMe123! 时代的，调 PUT /system/users/{id}/reset-password 全部重置为 123456 并验证登录成功；web 重置密码后端取 security.default-password(DEFAULT_USER_PASSWORD 未设置→123456)，本身已是 123456 无需改码。坑：reset-password 是 PUT 不是 POST。

- 零售商可见门店签收页：menuConfig.ts 父分组'分割配送'补 RETAILER 角色（子菜单/路由 meta 原本已放行，仅父级拦截）；实测 RETAILER token 调 /distribution/receipts 返回 200，后端无角色限制。

- 配送商点菜单报无权限执行该操作：根因是 TransportMonitor/CarcassBatch/PigList 页面加载时调 GET /system/orgs/tree，auth-service SecurityConfig 将 /system/** 全限 ADMIN 导致 403。修复：SecurityConfig 在 /system/** 之前加 requestMatchers(/system/orgs/tree).authenticated()（机构树为公共基础数据），重建 auth-service（healthy）。配送商 token 实测 orgs/tree=200、/system/users 仍 403。
- 配送商菜单收窄：menuConfig.ts + router/index.ts 将 distribution/batch、split、receipt 移除 DISTRIBUTOR（receipt 保留 RETAILER），配送商分组下仅剩冷链运输。
- 屠宰场角色(SLAUGHTER_OP)开放胴体批次+分割操作：router/index.ts 与 menuConfig.ts 的 distribution/batch、split 及父分组 roles 补 SLAUGHTER_OP；实测 slaughter_user token 调 orgs/tree、batches、splits 均 200。
- 登录页删除标题上方猪头 SVG 图标（LoginView.vue 移除 brand-logo 块及其 CSS）。
- ET-TEST-0017 状态仍显示已出栏：根因是 docker-compose 只配了 BREEDING_SERVICE_URL 系列（trace yml 映射用），而 sales/distribution/slaughter 三家的 RestClient 用 @Value(services.xxx-url) 属性键 → 解析到默认 localhost:8082，容器内调用必败且异常被吞（RestClientException 只记 warn，旧容器日志随重建丢失）。修复：x-app-environment 补 SERVICES_BREEDING_URL/SERVICES_SLAUGHTER_URL/SERVICES_DISTRIBUTION_URL/SERVICES_SALES_URL/SERVICES_BLOCKCHAIN_URL（Spring SystemEnvironmentPropertySource 会把 services.breeding-url 映射到 SERVICES_BREEDING_URL）；up -d 重建 4 容器均 healthy。并从 distribution 容器内调 breeding internal 接口把猪20(ET-TEST-0017)补到 status=3。验证：容器互调 200，DB 状态 0016/0017 均 3。
- 疫苗录入生猪下拉列出已屠宰猪：后端 /pigs 的 status 参数扩展为支持逗号分隔多状态（PigIndividualServiceImpl.parseStatus→parseStatuses，wrapper.eq 改 in）；前端 VaccineFormDialog.vue 搜索加 status='1,2'（只含在养/已出栏，排除已屠宰/异常）。重建 breeding-service（healthy），实测 status=1,2 仅返回在养猪 ET-TEST-0018，搜已屠宰 0017 返回 total=0。
- 新增瘦肉精检测耳标下拉无数据（RactoFormDialog.vue）：原搜索只查 status=2 且排除已有检测记录的猪，当前库内无 status=2 的猪 → 恒为空。改为 status='2,3'（与屠宰检验下拉同口径），并移除"排除已有检测记录"限制（不同检测项目可多次检测，防重由 testNo/sampleNo 唯一校验兜底）。纯前端改动，热更新生效；实测搜 0017/ET 均返回已屠宰猪。注：库内 id=17 的耳标号含前导空格" ET-TEST-0001"（脏数据，未处理）。
- 用户反馈瘦肉精弹窗下拉又出现已屠宰猪：RactoFormDialog.vue 搜索收窄回 status=2（仅已出栏），已屠宰(3)不再出现——与屠宰检验收窄逻辑对齐（瘦肉精无作废/重录场景，无 status=3 放行分支）。注意：当前库内无 status=2 的猪，下拉暂时为空，需新走一笔出栏审批后才有数据。
- 新增检疫盖章耳标下拉无数据（StampFormDialog.vue）：原规则"已屠宰猪仅在存在已作废盖章记录时出现"，但库里所有已屠宰猪的盖章记录均有效、且无 status=2 的猪 → 恒为空（连从未盖章的 ET-TEST-0018 也被挡住，首次盖章选不到猪）。修复：过滤规则简化为"排除已有有效盖章记录的猪，其余（已出栏/已屠宰的首次盖章+作废重录）均可选"。InspectFormDialog.vue 同样漏洞一并修复。实测：19 头已出栏/已屠宰猪中 18 头有有效盖章被排除，仅剩未盖章的 ET-TEST-0018 可选，符合业务。注：前次模拟脚本 regex 转义有误导致误判"无盖章"，已修正重测。

## 2026-09-18 10:20 上链范围盘点（未改代码，仅审计）
- 后端真实 publish 点（common-mq ChainTxMessage.create → rabbit pork.trace.chain → blockchain-service consume → blockchain_record）：
  - breeding-service QuarantineCertServiceImpl.uploadCert → bizType=QUARANTINE_CERT
  - slaughter-service SlaughterInspectionServiceImpl add/update → SLAUGHTER_INSPECT；voidInspection → SLAUGHTER_INSPECT_VOID
  - slaughter-service CarcassStampServiceImpl addStamp/updateStamp → 复用 SLAUGHTER_INSPECT；voidStamp → CARCASS_STAMP_VOID（⚠️ bizType 命名与"盖章"语义不符，前端 CARCASS_STAMP 标签永不出现）
  - distribution-service DistributionServiceImpl → SPLIT_BATCH(createSplitBatch)、STORE_RECEIPT(门店签收)
  - sales-service SalesServiceImpl → RETAIL_SALE(激活/售出)、RECALL_ORDER(创建召回)、RECALL_ORDER_PROGRESS(:PROGRESS 独立 bizKey)
- 前端 AuditLog/TraceSearch 的 operTypeMap 列了 15 种，但 PIG_INDIVIDUAL/VACCINE_RECORD/ENTRY_INSPECTION/RACTOPAMINE_TEST/CARCASS_STAMP/COLD_CHAIN_TRANSPORT 后端无任何 publish（假标签）。
- 上链数据页面入口：屠宰检验 /admin/slaughter/inspect（InspectTable 有编辑+作废，列表无链上状态列）、检疫盖章 /admin/slaughter/stamp（StampTable 有 BlockchainVerifyBadge + 编辑 + 作废）、分割操作 /admin/distribution/split（详情显"区块链状态"+txHash）、门店签收 /admin/distribution/receipt（链上状态列）、销售激活/记录（/admin/sales/qrcode、/sales/records）、产品召回 /admin/sales/recall；只读：审计日志 /admin/system/audit、追溯查询 /admin/trace/search（验真）、看板区块链上链量。
- 已上链记录的编辑实现：改库 UPDATE 后按**同一 bizKey** 重新 publish 新哈希（链上留版本、验真取最新）——等于"可编辑+追加存证"，不是作废重开。
- QUARANTINE_CERT 无 web-admin 页面入口（仅 api/modules/breeding.ts + stores/pig.ts，/breeding/pigs/{id}/quarantine-cert）。
- 小程序只读：pages/scan-result、pages/safe-buy、pages/verify（验真）、components/BlockchainBadge|CertChain。

## 2026-09-18 10:30 屠宰检验/检疫盖章：去掉编辑入口 + 去掉"待"状态（已改码）
- 决策依据：这两类记录已上链，编辑是"改库+同 bizKey 重发哈希"，审计上无法证明改了什么 → 收敛为"只能作废+重录"。
- 改动（纯 web-admin 前端 6 个文件）：
  - InspectTable.vue、StampTable.vue：删除行内"编辑"按钮（emit('edit') 保留未删，父页 handleEdit/@edit 与对话框 editData 分支成为死代码，本次按用户要求不动）。
  - SlaughterInspect.vue / CarcassStamp.vue：搜索面板状态下拉去掉"待检验"/"待盖章"。
  - InspectFormDialog.vue：状态选项只留 合格(1)/不合格(2)，默认值与 resetForm 由 0 改为 1。
  - StampFormDialog.vue：状态选项只留 已盖章(1)，默认值与 resetForm 由 0 改为 1。
  - 列表 label map（0=待检验/待盖章）**保留**，用于显示历史脏记录。
- 注意：默认值选 1（合格/已盖章）而非 null，因为后端 parse(null)→null，而 trace-service inspectionPass 以 status==1 作兜底，null 会导致小程序"安心购"误判检验未通过。
- 遗留未做（用户明确只要求改前端）：后端 PUT /slaughter/inspections/{id}、/slaughter/stamps/{id} 仍可改库并重发哈希；SlaughterStatus 仍接受"待检验/待盖章"；api/modules/slaughter.ts 的 update 方法成死代码。

## 2026-09-18 11:00 出栏审批驳回逻辑核查（未改码，只答问）
- `SlaughterApplyServiceImpl.approve()`：只有 `approved=true` 且猪原状态为 null/1 时才把 `pig_individual.status` 推进为 2 已出栏；**驳回分支只更新申报记录（approval_status=2 + reject_reason + approver + approval_time），完全不碰生猪档案 → 驳回后猪仍是在养(1)**。createApply 也不动猪状态。
- ⚠️ 连带死路：驳回后该猪无法重新申报（createApply 先查 exists>0 即抛"该生猪已存在出栏申报记录"，DB 另有 `slaughter_apply` UK `uk_pig(pig_id)` 兜底），且被驳回记录不能再审批（approve 对 approvalStatus!=0 抛"该申报已审批，不能重复操作"）→ 猪永久卡在"在养 + 一条驳回申报"。申报入口仅在 生猪档案 PigList.vue 的"出栏申报"弹窗。出栏申报未上链（breeding-service 只有 QUARANTINE_CERT 会 publish），改动不涉及链上。

## 2026-09-18 11:05 修复"驳回后无法重新申报"（已改码+已部署+已实测）
- 改 `SlaughterApplyServiceImpl.createApply`：原"已存在申报就抛错"改为按 approval_status 分流——待审(0) 抛"请等待审批结果"、已通过(1) 抛"不能重复申报"、**已驳回(2) 允许在同一行上覆盖重提**（lambdaUpdate 重置 applyNo 为新号、applyTime、weightKg、targetSlaughterhouse、approvalStatus=0，并把 approver/approvalTime/rejectReason 显式置 null——注意必须用 update wrapper 的 .set(col,null)，updateById 会跳过 null 字段）。不需要改 DDL，uk_pig 仍是一猪一行。
- 部署：`cd backend && docker compose up -d --build breeding-service`（容器内 Maven 全量构建约 4 分钟，日志里 "#N BUILD SUCCESS" + "Container ... Recreated" 即成功）。实测：容器 healthy；`POST /breeding/pigs/8/apply`（该猪原本是 id=8 驳回记录）返回 200，复查 detail → status=0 待审、applyNo 换成新号、rejectReason/approver 已清空；随后再次驳回作为清理。
- 网关前缀是 **/api/v1**（nginx.conf `location /api/v1/`），登录 `POST /api/v1/auth/login {username,password}`（admin/123456）→ data.token；直接打 8080 或 /api 都 404。
- 遗留（未做）：① 驳回原因只有"出栏审批"页（SUPERVISOR/ADMIN）能看，养殖户看不到，且覆盖重提会清掉该原因——如需给养殖户看要新增"按 pigId 查申报"接口 + 申报弹窗内提示；② 想要"驳回留痕（多次申报历史）"必须去掉 uk_pig 改一猪多行（要同步改 init-sql/03-breeding.sql + 线上 DDL）。
- 测试副作用：演示数据 slaughter_apply id=8(pig 8) 的 applyNo 由 SA-TEST-0007 变为 SA20260918161060，驳回原因被我改成了 "auto-test cleanup"（原值不可恢复）。